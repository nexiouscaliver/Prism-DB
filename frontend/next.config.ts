/**
 * PrismDB Frontend Next.js Configuration
 * 
 * This file configures the Next.js application for the PrismDB frontend.
 * It specifies build-time settings and can be extended with additional 
 * options as needed for deployment and optimization.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
